name: app_clinica
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # (Atualizei para um intervalo mais moderno, mas pode manter o seu)

dependencies:
  flutter:
    sdk: flutter

  # Pacotes que adicionamos
  http: ^1.1.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.1
  intl: ^0.18.1 # (Atualizei para uma versão estável mais comum)
  table_calendar: ^3.0.9 # (Atualizei para uma versão estável mais comum)
  path_provider: ^2.1.3 # (Atualizei)
  open_file: ^3.3.2 # (Atualizei)

  # Pacote padrão
  cupertino_icons: ^1.0.2

# --- CORREÇÃO DE INDENTAÇÃO AQUI ---
dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^4.0.0
# --- FIM DA CORREÇÃO ---

# --- CORREÇÃO DE INDENTAÇÃO AQUI ---
# Este bloco deve estar no nível raiz (sem espaços à esquerda)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
# --- FIM DA CORREÇÃO ---

flutter:
  uses-material-design: true

  # (O resto do seu ficheiro flutter: ... assets: ... fonts: ... etc.)